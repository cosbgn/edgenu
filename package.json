{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "edge:dev": "npx wrangler pages dev --port 3000 --log-level=debug --compatibility-date=2023-08-07 -- nuxt dev"
  },
  "devDependencies": {
    "@auth/core": "^0.14.0",
    "@hebilicious/authjs-nuxt": "^0.3.0",
    "h3": "1.8.0-rc.3",
    "nuxt": "^3.7.1"
  }
}
